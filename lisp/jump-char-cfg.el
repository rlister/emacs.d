;; jump-char from melpa
(global-set-key (kbd "M-j")   'jump-char-forward)
(global-set-key (kbd "M-S-J") 'jump-char-backward)

;; then repeat char to keep going, ;/, for forw/back,
;; and C-c C-c to switch to ace-jump with char
